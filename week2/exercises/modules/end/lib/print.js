/**
 * Show something to the outer world
 */

const printAverageScoreForStudent = (name, averageScore) => {
  console.log(`Student ${name} has an average score of ${averageScore}`);
}

export default printAverageScoreForStudent;